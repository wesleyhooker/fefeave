{
  "name": "fefeave",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "test:project-head": "node scripts/test-project-head.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "prettier": "^3.2.4"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json,md,yml,yaml,css,scss,html}": [
      "prettier --write"
    ],
    "backend/**/*.{ts,js}": [
      "node scripts/lint-staged-eslint.js"
    ]
  }
}
