/**
 * Public marketing site shell.
 *
 * Applied on `(public)/layout`. Scopes accent tokens in `tokens.css` (`.public-site`)
 * so admin and other routes keep the global defaults.
 */
export const publicSiteClass = 'public-site';

/** Body copy on public pages — full charcoal for stronger readability. */
export const publicProseBodyClass = 'text-fefe-charcoal leading-relaxed';
