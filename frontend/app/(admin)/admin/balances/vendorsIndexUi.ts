/**
 * Vendors index — dedicated hub hero illustration (page-local).
 */
export const VENDORS_INDEX_HERO_ILLUSTRATION_SRC =
  '/images/vendors/vendors-hero.png';

/** Next/Image intrinsic dimensions — 1536×1024 wide scene asset. */
export const VENDORS_INDEX_HERO_ILLUSTRATION_INTRINSIC = {
  width: 1536,
  height: 1024,
} as const;

/** Recent payments rail — empty-state illustration. */
export const VENDORS_INDEX_RAIL_EMPTY_ILLUSTRATION_SRC =
  '/images/vendors/vendors-recent-payments.png';

/** Next/Image intrinsic dimensions — 1024×1024 square asset. */
export const VENDORS_INDEX_RAIL_EMPTY_ILLUSTRATION_INTRINSIC = {
  width: 1024,
  height: 1024,
} as const;
